<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="ToDoList" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="dgToDoList" FontSize="18" FontWeight="DemiBold" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path = CreationData}" IsReadOnly ="True" Header ="CreationData" Width = "170"/>
                <DataGridCheckBoxColumn Binding="{Binding Path = IsDone, UpdateSourceTrigger = PropertyChanged}" IsReadOnly ="False" Header="Done" Width = "70"/>
                <DataGridTextColumn Binding="{Binding Path = Text}" IsReadOnly = "False" Header = "NeedToDo" Width = "*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
